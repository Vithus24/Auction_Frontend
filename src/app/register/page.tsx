import RegisterForm from "@/components/RegisterForm";

export default function RegisterPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-[url(/bg1.jpg)]  bg-custom bg-cover bg-center">
      <RegisterForm/>
    </div>
  );
}